##### 9월 27일
# 문자열을 정수로 바꾸기
# 정수 내림차순으로 배치하기
# 정수 제곱근 판별
# 문자열 P와 Y의 갯수


# 리스트의 원소 개수
len(list)

# 리스트의 원소의 합
sum(list)

# 루트 표현
a**(1/2)

# 리스트에 추가
list.append()

# 리스트 역순
list.reverse()

# 문자열 집계함수 count
s = 'asdaas'
s.count('a') # 3

# join 함수
answer = "".join(a) # 문자열 리스트를 붙여서 걍 문자열로 만듬 int() 하면 정수로도 바꿀 수 있을걸요

# 숫자끼리 떼어서 리스트로 정렬
while n > 0:
        r = n % 10
        n = n // 10
        lst.append(r)
        
# string에서 - +를 부호로 인식 : 문자열을 정수로 바꾸기
def strToInt(str):
    
    a = int(str)
    return a

